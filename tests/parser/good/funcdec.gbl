fn nothing() { }
fn greet(name: string) { }
fn add(a: int, b: int) -> int { return a + b; }
fn all(a: int, b: float, c: string, d: bool, e: dollars, f: chance, g: streak) -> bool { return true; }
fn body(x: int) -> int {
	var y = x + 1;
	return y;
}
